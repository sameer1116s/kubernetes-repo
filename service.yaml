apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80         # The port the service exposes externally
      targetPort: 80   # The port the container is listening on
  type: ClusterIP      # Changes to NodePort or LoadBalancer if needed
